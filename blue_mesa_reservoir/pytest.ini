[pytest]
# Puts this folder on the import path so `import data_loader` works when
# pytest is run from the repository root (pixi always runs it with
# cwd = "blue_mesa_reservoir", but this keeps `pytest` alone working too).
rootdir = .
